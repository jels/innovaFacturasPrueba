import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'piloto-facturas-standalone',
  webDir: 'www'
};

export default config;
